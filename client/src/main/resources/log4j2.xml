<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Appenders>
        <!-- Файловый аппендер для записи логов в файл -->
        <File name="File" fileName="logs/appClient.log" append="true">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%p] %c{1} - %msg%n" />
        </File>
    </Appenders>

    <Loggers>
        <Root level="debug">
            <!-- Подключаем только файловый аппендер -->
            <AppenderRef ref="File"/>
        </Root>
    </Loggers>
</Configuration>
